import img from './Error.gif';

const ErrorMessage = () => {
    return (
        <img style={{margin: '0 auto', background: 'none', display: 'block'}} src={img} alt="Error"/>
    )
}

export default ErrorMessage;