import img from './spinner.gif'

const Spinner = () => {
    return (
        <img style={{margin: '0 auto', background: 'none', display: 'block'}} src={img} alt="Spinner"/>
    )
}

export default Spinner;